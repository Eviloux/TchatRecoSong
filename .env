########################################
# Backend FastAPI
########################################

# URL de connexion PostgreSQL. Renseignez la valeur fournie par Render (ou Neon)
# pour le service de base de données. Exemple :
# postgresql://user:password@host:5432/database?sslmode=require
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# ℹ️ Render expose aussi `DATABASE_INTERNAL_URL` et `POSTGRES_INTERNAL_URL`. Si vous
# disposez de ces variables, vous pouvez les coller directement ici à la place de
# `DATABASE_URL`.
# DATABASE_INTERNAL_URL=postgresql://user:password@dpg-xxx.frankfurt-postgres.internal:5432/database

# En dernier recours, renseignez les champs individuels ci-dessous :
# DATABASE_HOST=dpg-xxx.frankfurt-postgres.render.com
# DATABASE_PORT=5432
# DATABASE_USER=render_user
# DATABASE_PASSWORD=render_password
# DATABASE_NAME=render_db
# DATABASE_SSLMODE=require

# Domaines autorisés à appeler l'API (séparés par des virgules). Mettez l'URL
# de votre frontend public et, si besoin, l'URL d'administration.
CORS_ORIGINS=https://wizbit.example,https://admin.wizbit.example

# Secret utilisé pour signer les JWT administrateur. Choisissez une chaîne
# longue et aléatoire et gardez-la secrète.
ADMIN_JWT_SECRET=change-me-in-production

# Durée de validité des jetons administrateur en minutes. 720 = 12 heures.
ADMIN_TOKEN_TTL_MINUTES=720

# Identifiants clients OAuth Google et Twitch utilisés pour vérifier les
# connexions côté backend (jetons envoyés par le frontend).
GOOGLE_CLIENT_ID=google-oauth-client-id.apps.googleusercontent.com
TWITCH_CLIENT_ID=twitch-client-id

# Comptes autorisés à accéder à l'administration. Séparez chaque entrée par
# des virgules.
ALLOWED_GOOGLE_EMAILS=admin1@gmail.com,admin2@gmail.com
ALLOWED_TWITCH_LOGINS=admin_twitch_1,admin_twitch_2

########################################
# Frontend Vue (Vite)
########################################

# URL publique de l'API (celle du backend hébergé sur Render).
VITE_API_URL=https://tchat-reco-backend.onrender.com

# Identifiants clients utilisés côté frontend pour initialiser les boutons de
# connexion Google et Twitch. Ils doivent correspondre aux valeurs backend.
VITE_GOOGLE_CLIENT_ID=google-oauth-client-id.apps.googleusercontent.com
VITE_TWITCH_CLIENT_ID=twitch-client-id

# URL publique à montrer dans le tchat Twitch (par exemple via !reco). Cette
# valeur est reprise dans l'interface utilisateur.
VITE_PUBLIC_VIEWER_URL=https://tchatrecosong-front.onrender.com
