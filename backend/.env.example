# =========================
# Backend environment file
# =========================
# Copie ce fichier en `.env` puis remplis chaque valeur avant de lancer le backend
# ou de configurer les variables d'environnement sur Render.

# URL complète PostgreSQL fournie par Neon ou Render (préférée)
# Exemple : postgresql://user:password@host:5432/database?sslmode=require
DATABASE_URL=

# -- Alternative : renseigne les champs individuels si tu n'as pas d'URL unique --
#DATABASE_HOST=
#DATABASE_PORT=5432
#DATABASE_NAME=
#DATABASE_USER=
#DATABASE_PASSWORD=
#DATABASE_SSLMODE=require

# Domains autorisés pour CORS (séparés par des virgules)
CORS_ORIGINS=https://tchatrecosong-front.onrender.com

# Secret utilisé pour signer les JWT administrateurs (génère une longue chaîne aléatoire)
ADMIN_JWT_SECRET=change-me
# Durée de validité des tokens admin en minutes (12 h par défaut)
ADMIN_TOKEN_TTL_MINUTES=720

# Identifiants OAuth Google & Twitch (obtenus dans les consoles développeur)
GOOGLE_CLIENT_ID=
TWITCH_CLIENT_ID=

# Liste des comptes autorisés à accéder à l'administration
# Sépare les valeurs par des virgules (ex: alice@example.com,bob@example.com)
ALLOWED_GOOGLE_EMAILS=
ALLOWED_TWITCH_LOGINS=

# Durée de vie des tickets !reco en secondes (optionnel)
TICKET_TTL_SECONDS=900

# Niveau de log (info, debug, warning...)
LOG_LEVEL=info
